name: EdgeTX TX12 MKII ELRS SAFE BUILD

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      # -------------------------
      # CLEAN
      # -------------------------
      - name: Clean
        run: |
          rm -rf build
          mkdir build

      # -------------------------
      # CHECKOUT
      # -------------------------
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: EdgeTX/edgetx
          ref: v2.12.0
          submodules: recursive

      # -------------------------
      # TOOLCHAIN (официальный пакет)
      # -------------------------
      - name: Install deps
        run: |
          sudo apt update
          sudo apt install -y \
            gcc-arm-none-eabi \
            binutils-arm-none-eabi \
            libnewlib-arm-none-eabi \
            python3 \
            python3-pip \
            cmake \
            build-essential

      # -------------------------
      # PYTHON
      # -------------------------
      - name: Python deps
        run: |
          pip3 install --no-cache-dir pillow numpy lz4

      # -------------------------
      # CONFIGURE (КЛЮЧЕВОЙ МОМЕНТ)
      # -------------------------
      - name: Configure
        run: |
          cmake -S . -B build \
            -DPCB=X7 \
            -DLUA=ON \
            -DLUA_MODEL_SCRIPTS=ON \
            -DWIDGETS=OFF \
            -DTELEMETRY_LOG=OFF \
            -DVOICE=ON \
            -DSOUND=ON \
            -DCMAKE_BUILD_TYPE=Release

      # -------------------------
      # BUILD
      # -------------------------
      - name: Build
        run: |
          cmake --build build --target firmware

      # -------------------------
      # OUTPUT
      # -------------------------
      - name: Output
        run: |
          find build -name "*.bin"

      # -------------------------
      # UPLOAD
      # -------------------------
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: build/**/*.bin
