- name: Build TX12 firmware
  run: |
    cd edgetx

    mkdir build
    cd build

    cmake .. \
      -G "Unix Makefiles" \
      -DPCB=TX12 \
      -DLUA=YES \
      -DLUA_MODEL_SCRIPTS=YES \
      -DLUA_TELEMETRY_SCRIPTS=YES \
      -DCMAKE_BUILD_TYPE=Release

    make -j$(nproc)
